import Head from 'next/head'

// CHANGE LATER
export default function Header() {
  return (
    <Head>
      <title>CSE187 - ModerationApp</title>
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  )
}